Алерт: `PodInfoHightLatency`  
Severity: `error`  
Impact: Значительно выросло время ответа HTTP

1. Подтвердите проблему
   Откройте дашборд `Main dashboard` и убедитесь, что latency растет на панели `Latency P90/P95`.
   http://localhost:8080

2. Перезапустите Deployment командой
   ```
   kubectl rollout restart deployment podinfo -n demo-alert
   ```

4. Проверьте, что пользователям стало лучше  
   На дашборде время ответа должно заметно упасть

8. Закройте инцидент и оставьте след  
   В статусе инцидента зафиксируйте время рестарта или отката.
   Создайте задачу: «выяснить причину роста latency»
